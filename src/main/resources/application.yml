server:
  port: 8092

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    password: 5744
    url: jdbc:postgresql://localhost:5433/breakfast
    username: postgres

  liquibase:
    change-log: classpath:liquibase/main-changelog.yml
